version: 2.0
jobs:
  build:
    docker:
      - image: circleci/ruby:2.6.3
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
